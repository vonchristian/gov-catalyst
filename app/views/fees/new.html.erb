<%= simple_form_for [@business, @fee], url: :business_fees do |f| %>
  <%= f.input :name %>
  <%= f.input :amount %>
  <%= f.input :revenue_account_id, collection: Accounting::Revenue.all.map{|a| [a.name, a.id] } %>
  <%= f.submit %>
<% end %>
